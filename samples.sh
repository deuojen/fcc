 #! /bin/bash
 
 # echo -e "\nNumber of times cat, cats, or catnip appears:" >> kitty_info.txt
 # grep 'cat[a-z]*' kitty_ipsum_2.txt -o | wc -l >> kitty_info.txt 
 # grep 'cat[a-z]*' kitty_ipsum_2.txt -n | sed 's/([0-9]+).*/\1/' -E >> kitty_info.txt 

 # psql --username=freecodecamp --dbname=postgres
 # You can query the database in your script with psql --username=freecodecamp --dbname=salon -c "SQL QUERY HERE", add more flags if you need to. Be sure to get creative, and have fun!
 # backup ->  pg_dump -cC --inserts -U freecodecamp salon > salon.sql
 # restore -> psql -U postgres < salon.sql

 # chmod +x ./salon.sh